name: Project automation

on:
  issues:
    types: [opened]
  pull_request:
    types: [opened]

permissions:
  issues: read
  pull-requests: read

jobs:
  add-to-project:
    name: Add to project
    runs-on: ubuntu-latest
    steps:
      - uses: actions/add-to-project@v1.0.2
        with:
          project-url: https://github.com/users/zeevdr/projects/2
          github-token: ${{ secrets.PROJECT_TOKEN }}

